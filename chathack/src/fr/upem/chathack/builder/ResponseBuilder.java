package fr.upem.chathack.builder;

import java.nio.ByteBuffer;
import fr.upem.chathack.utils.Helper;

public class ResponseBuilder {
  private ByteBuffer bb = ByteBuffer.allocate(Helper.BUFFER_SIZE);
}
